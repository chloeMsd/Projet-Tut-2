#include "instance.h"

//le tableau en entrée contient tous les indices des objets, triés selon un certain ordre
int* obtenirSolutionDirecte(int* solutionOrdonnees, const struct Instance* instance)
{
    int* solutionDirecte = malloc(sizeof(solutionOrdonnees));

    for (int i = 0; i < instance->nombre_objets; i++)
    {
        
    }
}